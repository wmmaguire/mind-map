## Project status (MindMap / talk-graph)

Last updated: 2026-04-24 — **Google Sign-In via Google Identity Services (branch `issue-102-google-sign-in`, GitHub #102; first OAuth slice of epic #31):** adds one-tap, automatic return-visit, and branded "Continue with Google" sign-in on top of the existing email/password flow. Landed in four staged commits on one branch: **M1** `226a732` (server foundation), **M2** `9b8024c` (client button + AuthContext), **M3** `4a55f84` (One Tap + auto-select), **M4** (this entry, docs + backlog routing). **(1) `User` schema additive fields — `server/models/user.js`.** New `googleId` (sparse unique + index), `avatarUrl`, `emailVerified`, and `provider ∈ {'password','google'}`; `passwordHash` made optional so Google-only accounts can exist. Existing rows default to `provider: 'password'` with no migration. `toJSON` strips `googleId` but exposes `avatarUrl` + `provider` so the banner can render a Google badge / avatar. **(2) `server/lib/googleAuthClient.js` — thin wrapper around `google-auth-library.OAuth2Client.verifyIdToken`.** Exposes a factory `createGoogleAuthClient({clientId, allowedHd, verifyIdTokenFn})` with an injectable verifier so tests and dev exercise the full code path without a real Google client ID or network round-trip. Validates `aud`, `iss` (`https://accounts.google.com` or `accounts.google.com`), `sub`, `email` (normalized to lowercase), optional `nonce`, and optional `hd` workspace allow-list. Returns `{ok:true, payload}` / `{ok:false, status, code, error}` so routes don't need try/catch boilerplate. **13** unit tests in `googleAuthClient.test.mjs`. **(3) `server/lib/authService.js` — `resolveGoogleUserWithStore()` + link JWT helpers.** Pure, store-shaped resolver (matches `registerWithStore` / `loginWithStore`) returns one of three outcomes: `existing-google` (reissue session + patch changed `name`/`picture`), `link-required` (password row with matching email — returns a 5-min signed `link-google` JWT; **never auto-merges**), or `new` (creates `{provider:'google', passwordHash:null, emailVerified}` row). `signLinkGoogleToken({userId, googleSub, emailLower})` + `verifyLinkGoogleToken(token)` are stateless 5-min JWTs bound via `purpose: 'link-google'` so they can't be reused for any other endpoint. `loginWithStore()` now returns **`400 USE_GOOGLE_SIGN_IN`** when `provider === 'google'` + `passwordHash === null`, before bcrypt runs (prevents both account-enumeration timing leaks and confusing "invalid credentials" messages for Google-only users). **8** new tests in `authService.test.mjs` (3 outcomes, link-token round-trip, no-auto-merge guarantee, login-steering). **(4) `server/routes/auth.js` — four new endpoints.** `POST /api/auth/google/nonce` mints a 32-byte token and sets the 5-min httpOnly `mindmap_google_nonce` cookie so the server can cross-check the ID token's `nonce` claim on the exchange. `POST /api/auth/google` (`{credential, nonce}`) verifies + resolves + issues session cookie (or 409 `LINK_REQUIRED` + `{linkToken, email}`). `POST /api/auth/google/link` (`{linkToken}`) finalizes Google ↔ password linking after explicit user consent; guards against `GOOGLE_ID_CONFLICT`. `POST /api/auth/google/revoke` clears session + nonce cookies (client calls `google.accounts.id.disableAutoSelect()` separately). Dev ergonomics: when `GOOGLE_OAUTH_CLIENT_ID` is unset and no verifier is injected, the endpoints return **`503 GOOGLE_SIGN_IN_DISABLED`** so local dev without a Google client ID stays on email/password cleanly. `userPublicJson` gains `avatarUrl` + `provider`. **(5) `client/public/index.html` — async+defer GIS script tag** (`https://accounts.google.com/gsi/client`); loaded unconditionally, runtime guards hide UI when unconfigured. **(6) `client/src/lib/googleIdentity.js` — GIS readiness helpers.** `getGoogleClientId` (reads `REACT_APP_GOOGLE_OAUTH_CLIENT_ID`), `isGoogleSignInConfigured`, `isGoogleIdentityReady` (checks `window.google.accounts.id.initialize` is mounted), `waitForGoogleIdentity` (polls every 60 ms, rejects after 5 s). **(7) `client/src/context/AuthContext.jsx` — four new context methods.** `requestGoogleNonce()` (POST `/api/auth/google/nonce`), `signInWithGoogle({credential, nonce})` → `{outcome: 'signed-in'|'link-required', …}`, `linkGoogleAccount(linkToken)`, `disableGoogleAutoSelect()`. `logout()` now calls `disableGoogleAutoSelect()` so One Tap can't silently re-sign-in on the next page load. Context value exposes `isGoogleSignInConfigured` so consumers gate UI without re-reading `process.env`. **(8) `client/src/components/auth/GoogleSignInButton.jsx` + `.css` — branded primary CTA.** Renders via `google.accounts.id.renderButton()` so Google's official styling is used. Mints a fresh nonce per mount, feeds it to `google.accounts.id.initialize` with `ux_mode: 'popup'` + `use_fedcm_for_prompt: true` + `auto_select: false`, and wires the callback to `signInWithGoogle`. Surfaces the three outcomes via props: `onSuccess({user})`, `onLinkRequired({linkToken, email})`, `onError(err)`. Returns `null` when `REACT_APP_GOOGLE_OAUTH_CLIENT_ID` is unset (local dev still works on email/password). **(9) `client/src/components/auth/GoogleOneTap.jsx` — One Tap + auto-select on return visits.** Headless component mounted inside `AuthIdentityBridge` (`client/src/index.js`). Fires `google.accounts.id.prompt()` at most once per app boot when (a) `REACT_APP_GOOGLE_OAUTH_CLIENT_ID` is set, (b) `status === 'guest'`, (c) `document.visibilityState === 'visible'`, (d) the user hasn't dismissed within the last 24 h (`localStorage['mindmap.auth.onetap.dismissedAt']`). Init uses `auto_select: true` so return visitors with a single Google account + prior consent are signed in silently. Link-required credentials encountered via One Tap are **not** surfaced mid-page (would be surprising UX); user is left as guest and can complete linking from the explicit sign-in modal. The `prompt()` notification callback records `isNotDisplayed` / `isSkippedMoment` / `isDismissedMoment` in localStorage so the next boot respects the 24 h silence window. **(10) `client/src/components/GuestIdentityBanner.jsx` — modal rearrangement + link-required dialog.** When Google Sign-In is configured, `GoogleSignInButton` is the primary CTA inside the existing sign-in / register modal; the email / password form collapses behind an "Or sign in with email" disclosure. When unconfigured, the email form is shown by default (no regression). A new **link-required** card surfaces `"An account with <email> already exists. Link your Google account to it?"` with **Link** / **Cancel** buttons; Link calls `linkGoogleAccount(linkToken)` and closes the modal on success. **Security:** audience / issuer / expiry enforced by `google-auth-library`; nonce round-tripped via httpOnly cookie (`GOOGLE_NONCE_MISMATCH` on mismatch); no silent merge of password rows onto Google identities; session rotation done-by-construction (every sign-in overwrites the cookie with a fresh JWT); credential JWTs never logged in full (only `{sub, aud, emailLowerHash}` on the exception path). **Tests:** server **123 / 123** passing (up from 102; +21 for #102). Client **170 / 170** passing across **25 suites** (up from 155; +15 for #102). `googleIdentity.test.js` (4), `GoogleSignInButton.test.jsx` (6), `GoogleOneTap.test.jsx` (5). Server + client lint clean. **Docs:** new **`docs/auth-google.md`** with Google Cloud console walkthrough, env-var table, endpoint reference, security notes, troubleshooting matrix; **`server/READEME.md`** + **`client/README.md`** updated; this entry. **Follow-ups outside this ticket (tracked on GitHub — see `docs/github-backlog-issues.md`):** (a) **Disconnect Google settings UI** — add a "Disconnect Google" action under Settings that calls `/api/auth/google/revoke` + clears `googleId` from the row, gated on the user setting a fallback password first so they don't lose access; filed as a phase-2 backlog issue. (b) **UserActivity auditing for `AUTH_GOOGLE_LOGIN` / `_LINK` / `_REGISTER`** — the existing password auth routes don't write `UserActivity` rows either, so Google inherits parity; a future pass should fold all auth into the audit log (tracked on **#64**). (c) **Rate limiting for `/api/auth/google*`** — (tracked on **#78**). (d) **Server-contract test for the `/api/auth/google` router** with the injected verifier — pure `authService.test.mjs` covers the resolver branches but an integration test that exercises cookie handling + `LINK_REQUIRED` body + `USE_GOOGLE_SIGN_IN` via supertest would round out the story (tracked on **#78**). (e) **Avatar rendering in the banner** — `userPublicJson` now returns `avatarUrl` but `GuestIdentityBanner` doesn't yet render the Google profile picture (tracked on **#33**). (f) **Privacy disclosure page / route** — ticket mentions adding a note that Google One Tap is used and that `name`, `email`, `avatarUrl` are stored on sign-in; scoped inline to the auth-google doc + link-required card copy for now, full `/privacy` route deferred (tracked on **#33**). (g) **OAuth code flow + refresh tokens** — non-goal here; revisit when we need Google API access (Calendar / Drive); would let us call Google APIs on behalf of the user for future features (tracked on **#31**). (h) **E2E test via Playwright** — mock the GIS `callback(credential)` and assert navigation to `/visualize` + `X-Mindmap-User-Id` on the next library call (tracked on **#78** / **#104**).

Last updated: 2026-04-24 — **Stable D3 keyed data joins across playback scrubs (branch `issue-103-stable-data-joins`, GitHub #103; phase-2 follow-up to #89):** replaces the per-scrub hard DOM wipe with a keyed enter/update/exit join contract so existing communities stay put and only the new nodes/links for the current step gently fade onto the scene. Landed in six staged commits (**M1 → M6**) on one branch: **M1** `b2ecda4`, **M2–M4** `a1783b9`, **M5** `6081671`, **M6** `b9c9422`. **(1) Keyed-join helpers extracted — `client/src/utils/graphJoinHelpers.js`.** Two pure utilities (`ensureLayer(parentSel, className)` and `applyKeyedJoin(parentSel, selector, data, keyFn, hooks)`) wrap d3's `.data(..., keyFn).join(...)` pattern and expose explicit `onEnter` / `onUpdate` / `onExit` hooks. `ensureLayer` is idempotent (`selectAll(g.<class>).data([null]).join('g')`) so the canonical **`g.links-layer`** / **`g.nodes-layer`** / **`g.cluster-thumb-layer`** persist across every `updateVisualization()` call. `cssEscapeClass` refuses non-alnum-dash-underscore class names to keep composed selectors safe. New unit tests in **`graphJoinHelpers.test.js`** cover enter-only, update-only, exit, keyed rebind identity, and the jsdom SVG-selector caveat (no `:scope >`). **(2) Drop the hard wipe + fade transitions.** `updateVisualization()` no longer calls `g.selectAll('*').remove()`; keyed joins drive every rebind path (initial mount, zoom merge/split, community evolution, playback scrub). `onEnter` fades new survivors in at `ENTER_DURATION_MS = 450 ms` with `d3.easeCubicOut`; `onExit` fades out at `EXIT_DURATION_MS = 280 ms` before `.remove()`; both honour **`prefers-reduced-motion: reduce`** via the existing `prefersReducedMotion()` helper (zero-duration transitions). Link hover tooltip handlers (`handleLinkMouseover` / `handleLinkMouseout`) — which used to hang off a separate wider `.link-hover-area` line in the legacy wipe-on-render block — are now wired directly onto the canonical `<line class="link">` inside `linkJoin.onEnter` so they attach once per link and survive scrubs. **(3) `fx`/`fy` pinning for survivor continuity.** On every new playback scrub (`isNewPlaybackScrub` = token advanced past `lastPlaybackFadeTokenRef`), surviving communities get `fx`/`fy` pinned to their current `x`/`y` and released after `ENTER_DURATION_MS` so the d3 force tick doesn't fling them while new nodes ease in. `renderNodeDiscOrThumb()` was audited for idempotency — the `onThumbImageError` fallback now guards against double-demotion so repeated `updateVisualization()` calls on the same node don't leak duplicate `circle.graph-node-disc` / orphan `defs` (same category of regression as **#86**). **(4) Split the main effect so scrubs re-render into the existing DOM.** `updateVisualizationRef` caches the latest `updateVisualization` closure; a new scrub-only **`useEffect`** on `[playbackScrubToken]` calls `updateVisualizationRef.current()` without touching the main effect's `[data, selectedNodes, width, height, readOnly]` deps — i.e. a pure scrub keeps the same `<g class="graph-root">` DOM and the keyed joins are the sole source of truth for the delta. **(5) Legacy DOM excision (M5 dead-code guard).** Dropping the wipe in M3 exposed a latent leak: legacy `const node = g.selectAll('.node')…` / `linkGroups` / outer `simulation.on('tick', …)` blocks in the main effect had been harmlessly wiped every render by `g.selectAll('*').remove()`; without the wipe they leaked siblings next to the canonical layers (duplicate `<g class="node">` per community, competing click + drag handlers, a plain `r=20` circle stacked over the real thumbnail / disc). M5 excised all three blocks (≈230 removed lines) and re-wired the one user-visible feature that survived (link-hover tooltip) into the canonical link `onEnter`. Two permanent regression guards added: **`graph-root holds only canonical layer groups (M5 dead-code guard)`** (asserts only the three canonical `<g>` children live under `g.graph-root`) and **`node subtree has drag listeners wired (M5 drag regression guard)`** (asserts d3-drag `__on` array is populated on each `g.node`, since jsdom can't simulate pointerdown → `d3-drag.start` reliably). **(6) Fix the "existing nodes fly in from the periphery on every consecutive step" regression (M6).** Post-M5 user testing revealed that the real library playback path — `LibraryVisualize.displayGraph = useMemo(..., [playbackStepIndex])` — hands `<GraphVisualization>` a brand-new `data` object reference on every scrub step, which re-ran the main D3 setup effect and tore down `g.graph-root` + recreated the force simulation each time. Keyed joins saw every community as a fresh enter, and `d3.forceSimulation(data.nodes)` re-initialized node positions to d3's internal spiral pattern before forces pulled them into place — hence the jarring "fly in from periphery" frame on every consecutive step. **M1–M5 only covered the `playbackScrubToken`-changing path with stable data, which is not what the real app hits.** M6 makes the main effect idempotent across data-driven re-runs: **(a)** `g.graph-root` is now select-or-create (same DOM element survives data changes); **(b)** the community force simulation is reused across re-runs via `communityForceSimulationRef.current` — only geometry-dependent forces (`forceX`/`forceY` center, `forceCollide`) are refreshed each run so width/height changes still take effect; fresh mounts start the sim with empty nodes and let `updateVisualization()`'s `.nodes(visibleElements)` rebind be the single source of truth for the bound node set; **(c)** a dedicated mount-only `useEffect` owns final simulation teardown (on unmount) so main-effect cleanup no longer stops the sim between scrubs; **(d)** `graphTransformRef.current` is preserved across re-runs and reapplied to `g` after root reuse so user pan/scale stays visually stable during scrubs; **(e)** `initializeCommunities()` now reads positions from the live persistent simulation first (`sim.nodes()` when the bound objects have a `.nodes` array, i.e. communities not raw `data.nodes`) and falls back to `previousCommunityPositionsRef` only for true remount scenarios; **(f)** drop the dead outer `nodeMap` / `processedLinks` locals that used to feed a pre-bound `forceLink(processedLinks)` into the initial sim. Two new regression tests exercise the real playback path (fresh `data` object on every step, not just `playbackScrubToken` change): **`entering nodes are added; existing ones keep DOM identity across a data-changing scrub (M6)`** and **`consecutive data-driven scrubs keep g.graph-root and survivor DOM stable (M6)`**; the prior "entering nodes are added" test was also strengthened (it used to allow either "keyed join preserved DOM" *or* "setup effect rebuilt"; now it enforces survivor DOM identity across a data change). **Tests:** client suite **155 / 155** passing across **22 suites** (+1 vs pre-#103 baseline); `graphJoinHelpers.test.js` and 8 new `#103` regression guards inside `GraphVisualization.test.js`. Root `npm run lint` clean; `cd client && npm run build` clean at **228.04 kB gzip** (**-340 B** net vs pre-#103 baseline once M5 excision is accounted for; M6 is +73 B over M5). **Docs:** this entry; **`client/README.md`** gains a bullet for **`graphJoinHelpers.js`** + a **`#103`** sub-bullet under **`GraphVisualization`**; **`docs/github-backlog-issues.md`** picks up a *Stable D3 keyed data joins (#103)* section with shipped slice + follow-ups + suggested issue comments. **Follow-ups outside this ticket (tracked on GitHub — see `docs/github-backlog-issues.md`):** (a) **Effect architectural split** — the main D3 `useEffect` is now re-entrant across data changes but still ~2000 lines, and a pure data change still re-runs most of the effect body (scope: rebuild `processedLinks` inside `updateVisualization()`, re-register zoom handler, etc.); splitting into mount-only setup / data-update / size-update / scrub effects is the next phase and already tracked on **#51** + **#94** (add comments elaborating the M6 learnings). (b) **`LibraryVisualize.displayGraph` memo identity** — returns a new `{ nodes, links }` object reference on every `playbackStepIndex` change even when the resulting graph is structurally identical, so scrubbing to the same step twice still triggers a main-effect re-run; candidate for a content-hash memo or a `useMemo` on `(committedGraph, playbackStepIndex)` with deep-equality short-circuit (tracked on **#104**). (c) **`previousCommunityPositionsRef` is now mostly redundant** with the persistent simulation — it's only exercised on true remounts; cleanup candidate (tracked on **#104**). (d) **Main-effect + scrub-only-effect double-call of `updateVisualization` on data-changing scrubs** — benign (survivors stay; re-render is idempotent) but wasteful; would naturally fall out of the architectural split on **#51**. (e) **Reduced-motion — force simulation continues to tick even though the alpha is clamped** — for strict reduced-motion users we could settle the sim on mount and skip the reheat entirely; **#57** a11y follow-up. (f) **D3 canvas integration tests under Playwright** — our M5/M6 regression guards assert DOM identity + d3-drag `__on` wiring, but jsdom can't advance rAF so true tick-driven position assertions need a real browser; folded into **#104** scope (originally scoped on closed **#24**). (g) **Drag vs `fx`/`fy` pin race during a new scrub** — M4 pins survivors' `fx`/`fy` and releases them after `ENTER_DURATION_MS`; if a user starts dragging mid-pin, the release may clobber drag's own `fx`/`fy` write (narrow edge case in touch-heavy workflows); **#104**.

Last updated: 2026-04-24 — **D3 force dynamics + playback continuity (branch `issue-89-disjoint-force-playback`, GitHub #89):** first pass on **#89** *Improve D3 force dynamics and playback transitions (disjoint layout)*. Addresses the "popping"/collapse behavior we still saw during playback scrubs and step transitions, focused on the community-force simulation in **`client/src/components/GraphVisualization.js`**. **(1) New util + tests — `client/src/utils/graphLayoutComponents.js`.** Exports **`computeConnectedComponents(nodes, links)`** (union–find, deterministic lowest-id component ids, link-endpoint resilient to object / raw ids, ignores links to missing nodes), **`computeComponentCentroids(nodes, components, fallbackCenter)`** (mean `x/y` over positioned nodes per component; treats `(0,0)` and `NaN` as unpositioned; falls back to viewport centre), and **`seedPositionsForNewCommunities(communities, links, fallbackCenter, { jitterRadius = 24 })`** — for any community without a valid `(x,y)`, seed it near its connected-component's centroid with a **deterministic hash-of-id jitter** (stable across re-renders, no `Math.random` so the same scrub always resolves to the same seed). **13** unit tests cover empty/single/multi-component graphs, orphan nodes, position inheritance, determinism, and `(0,0)` / `NaN` sentinel handling. **(2) Position + velocity snapshot across rebuilds.** Added **`previousCommunityPositionsRef`** in **`GraphVisualization`**. Every `updateVisualization()` entry captures `{ x, y, vx, vy }` for every current simulation datum (keyed by community id) *before* it calls `simulation.stop()` and wipes the DOM. **`initializeCommunities()`** then restores those positions+velocities onto the fresh community map, so unchanged nodes stay where the sim had already converged instead of snapping back to each source node's authored `x/y`. Reset on `playbackScrubToken === 0` (live graph → don't carry scrub residue). **(3) Component-biased seeding for brand-new communities.** After `communitiesRef.current` is rebuilt and `processedLinks` is computed, `seedPositionsForNewCommunities(visibleElements, processedLinks, { x: width/2, y: height/2 })` runs so newly visible nodes on a scrub advance materialise next to their connected neighbours instead of all stacking at the viewport centre and then exploding outward — matches the Observable [disjoint force-directed graph](https://observablehq.com/@d3/disjoint-force-directed-graph/2) pattern. **(4) Collide radius scales with community size.** **`forceCollide`** was a flat **`radius(50)`** for single nodes *and* merged clusters; replaced with **`communityCollideRadius(d)`** that returns **`initialCommunityRadius(d) + COMMUNITY_SIM_COLLIDE_PADDING`** (20 for single nodes, up to ~200 for merged clusters, plus **8 px** padding) and bumped iterations to **2** for crisper separation. Fixes merged-cluster overlap after splits and keeps per-component spacing honest. **(5) Gentler reheat on playback scrubs + reduced-motion floor.** **`simulation.alpha(0.3).restart()`** became a bucketed reheat: structural updates (zoom merge/split, AI generation, etc.) keep **`0.3`**; **`isNewPlaybackScrub`** drops to **`COMMUNITY_SIM_ALPHA_PLAYBACK_SCRUB = 0.12`** so converged positions aren't flung; **`prefers-reduced-motion: reduce`** clamps further to **`COMMUNITY_SIM_ALPHA_REDUCED_MOTION = 0.04`**. New **`prefersReducedMotion()`** helper wraps `matchMedia` with a `try/catch` for non-browser test envs. `alphaTarget(0)` called explicitly so previous scrub reheats don't leak. **(6) Docs / backlog.** This entry; **`client/README.md`** gains bullets for the new util + the three force-sim behavioural changes under `GraphVisualization.js`. **Tests:** new **`graphLayoutComponents.test.js`** (13/13); existing suites unchanged — **full client suite 137/137 tests in 21 suites**; root **`npm run lint`** clean; **`cd client && npm run build`** clean (+1.14 kB gzip for the util + tweaks). **Follow-ups outside this slice (tracked on GitHub):** (a) **Avoid full-DOM wipe during playback scrubs** — today `updateVisualization()` still does `g.selectAll('*').remove()` then re-renders the whole `.graph-root`; a staged data-join with `enter/update/exit` + `transition().ease()` (as scoped by **#86** before it was reverted for thumbnail regressions) would remove the final visible "flicker" on scrubs (still on **#86**'s follow-up list / **#89**'s scope but deferred here to keep the blast radius small); (b) **Rename `COMMUNITY_SIM_*_EXPLODE` constants to neutral target-stretch names** now that they're shared across Explode / Extend / Manual generate / community evolution / branch extrapolation (noted earlier on **#68** / **#69**); (c) **Per-component gravity ring** — instead of one global `forceX/Y` toward `(width/2, height/2)`, group the current visible elements by component and add per-component `forceX(cx).strength(≈0.05) + forceY(cy).strength(≈0.05)` centred on each component's centroid, which is the next phase of the disjoint-layout work; (d) **`GraphVisualization` integration test for playback continuity** — jsdom can't exercise D3 force math but can assert that the pre-rebuild snapshot and post-rebuild seed helper are called with the expected args on a `playbackScrubToken` bump; (e) **Configurable playback easing duration** — pair with **#70** phase 2 *configurable play delay* so the easing window matches the play interval.

Last updated: 2026-04-24 — **Mobile UX polish + landing visual quick wins (branch `issue-90-mobile-ux-polish`, GitHub #90 + #76):** first pass on **#90** *Backlog: Mobile-friendly UX/UI polish* plus a follow-on facelift on **`LandingPage`** for **#76**. **(1) Graph tooltip — mobile close affordance.** **`GraphVisualization`** renders every D3 tooltip through a new **`withTooltipChrome()`** helper that adds an accessible **`✕`** close button (**`.graph-tooltip-close`**) and normalises padding; close is wired through **`onTooltipWrapInteraction`** so pointer + keyboard both dismiss. **`GraphVisualization.css`** gains a **`@media (max-width: 480px)`** block that clamps tooltip width, bumps tap targets to **44×44**, and keeps the close affordance anchored. One stray **`<a>` → `<button type="button">`** swap to clear a residual **`jsx-a11y/anchor-is-valid`** warning. **(2) Modal — small-viewport fit.** **`Modal.css`** picks up **`padding: 16px`** + **`box-sizing: border-box`** on **`.modal-overlay`** and a **`@media (max-width: 600px)`** override for **`max-height`** + **`align-items`** so every **`<Modal>`** in the app fits narrow viewports. **(3) `BannerActionsDrawer` — new component.** **`client/src/components/BannerActionsDrawer.jsx`** (+ **`.css`**) is a right-edge slide-out **`<aside role="dialog">`** that hosts the banner's Account / Library / View sections on narrow viewports. Opens on tap of a **`☰`** menu chip in the banner trailing area or on right-edge **touchstart → touchmove(left) → touchend** swipe; closes via overlay tap, **Escape**, or route change. Focus moves to the drawer's close button on open and returns to the trigger on close; **`aria-expanded`** / **`aria-haspopup="dialog"`** / **`aria-controls`** wired; **`env(safe-area-inset-*)`** respected. **(4) `GuestIdentityBanner` — drawer integration + section reorder.** **`drawerOpen`** state + **`mobileMenuTriggerRef`**; **`useEffect`** auto-closes on route change and listens for right-edge swipes. Both the guest and registered variants render the **`☰`** chip and a **`<BannerActionsDrawer>`**; opening the drawer programmatically closes any open desktop popover to prevent stacking. Drawer sections reordered **Account → Library → View** (top-down priority for signed-in and guest paths). A **📚 Open Library** button lives *inside* the drawer's Library section on **`/visualize`** (delegates to **`LibraryUiContext.openMobileLibrary()`**), and the original leading **📚 Library** chip is hidden at **`max-width: 36rem`** via **`.guest-identity-banner .library-mobile-rail--banner`** so the control isn't duplicated on mobile. **(5) `LibrarySidebar` — dark theme promoted globally.** **`LibraryVisualize.css`** previously shipped a large mobile-only dark-theme block; split into (a) layout/animation rules kept inside **`@media (max-width: 768px)`** and (b) a palette layer scoped to **`.library-visualize .sidebar`** (+ descendants) that applies to **both desktop and mobile**, matching **`BannerActionsDrawer`**. Contrast fixes: **`.file-list-header`** flattened (no gradient/border/shadow bleed-through), **`.file-item:hover`** / **`.saved-graph-item:hover`** gained dark hover states, **`.saved-graph-item .graph-info small`** takes **`display: block`** so metadata wraps per line, and **`.file-list-header__actions`** / **`.saved-graphs`** / **`.saved-graph-item`** use staggered **`rgba(255,255,255, …)`** tints (**`0.05`** → **`0.09`** → **`0.13`** on hover) to create a lifted tray → container → item hierarchy on the dark surface. Scrollbar tokens (**`scrollbar-color`**, **`::-webkit-scrollbar*`**) aligned with the palette; desktop **`.sidebar-resize-handle`** restyled to match. **(6) Landing page — reflection as disclosure + hero polish (GitHub #76).** **`LandingPage.jsx`** relocates **`.landing-reflection`** from the bottom of the page to directly above **`.landing-value-loop`** and wraps the body in a **`<details class="landing-reflection-disclosure">`** summary-as-button pattern (summary reads **Why MindMap?**). The verbose closing paragraph is trimmed to *"Creative thinking wanders. MindMap captures these threads so you can uncover what drives you and discover new ideas."* Six quick wins follow: (a) **`.landing-hero-title`** dropped **`text-transform: uppercase`** and picked up a blue gradient via **`background-clip: text`** so the brand reads as **MindMap** at **`clamp(2.25rem, 5vw, 3.5rem)`**; (b) the hero's 1-px slate bottom border replaced by a blurred linear-gradient glow (**`.landing-hero::after`**); (c) **`.landing-content`** is now a flex column with **`--landing-section-gap: 2rem`** (1.5rem on ≤600px) and section margins zeroed so spacing is centralised; (d) second CTA at page bottom picked up a new **`.landing-cta-primary--outro`** modifier (pulse disabled via specificity chain against **`.landing-cta-primary--dynamic`**) plus a reassuring hint *"Your first graph takes about 60 seconds. No sign-in required."*; (e) **`FEATURE_CARDS`** entries tagged with an **`accent`** prop (blue / violet / amber) that drives a **`landing-feature-card--${accent}`** modifier on the **`<li>`**, and CSS scopes the icon gradient + shadow per accent so the grid reads as *input → interaction → output* instead of six blue tiles; (f) double-space typo in the tagline collapsed. **(7) Landing text-align regression fix.** **`.content { text-align: center }`** from **`App.css`** was cascading down to the landing reflection paragraphs because **`.landing-content`**'s single-class override shared specificity (**`0,1,0`**) and could lose on source order; rule promoted to **`.landing-container .landing-content`** (**`0,2,0`**) and the **`@media (max-width: 600px)`** override mirrored so mobile padding + **`--landing-section-gap`** still apply. **Tests:** **`LandingPage.test.jsx`** assertion updated from the old *Why it matters* heading to the new *Why MindMap?* summary text; **`criticalPath.integration.test.js`** **`getByText(/MindMap/i)`** queries tightened to **`getByRole('heading', { level: 1, name: /^mindmap$/i })`** since *MindMap* now appears twice on the page (hero + summary). **Full client suite:** **20 suites / 124 tests passing**; root **`npm run lint`** clean; **`cd client && npm run build`** clean. **Docs:** this entry; **`docs/github-backlog-issues.md`** (new *Mobile UX polish + landing quick wins* section with shipped slice + follow-ups + suggested issue comments); small bullet additions to **`client/README.md`** for the new **`BannerActionsDrawer.jsx`** component and the drawer surface on **`GuestIdentityBanner`**. **Follow-ups outside this change (tracked on GitHub):** **#90** remains **in review** with *phase-2* items (graph-touch drag/zoom, tap-to-toggle tooltips, playback-strip mobile density, sidebar drawer gestures, modal sticky footers, input-zoom prevention, a11y audit); **#76** remains **in review** with landing-page structural items (graph-aware hero, tonal bands, horizontal How-it-works, product mock, palette tokens, decouple from **`.content`** card). Two new backlog issues filed for items that don't belong to #90 or #76: *shared menu-item source of truth for mobile drawer + desktop popovers* and *`LibrarySidebar` palette tokens / optional light variant*.

Last updated: 2026-04-20 — **Graph insights assess — DeLanda default voice, Reflection↔Discovery slider, responsive Insights controls:** three related passes on the **Insights** panel (**View → Insights**, GitHub **#83**) and **`POST /api/graph-insights-assess`**. **(1) Voice preset — Manuel DeLanda added and made fixed default.** **`TONE_IDS`** in **`server/lib/graphInsightsAssess.js`** grew to `['freud', 'palahniuk', 'murakami', 'thompson', 'delanda', 'custom']`; **`TONE_SYSTEM_HINTS['delanda']`** describes a "lucid, systems-oriented voice inspired by Manuel DeLanda" that foregrounds **assemblages**, **emergent capacities**, **bifurcation**, **stratification**, and **morphogenesis** without collapsing into jargon. **`INSIGHT_ASSESS_TONE_OPTIONS`** in **`client/src/utils/graphInsights.js`** mirrors the id list for contract parity. The client-side **Perspective** `<select id="insights-assess-tone">` in **`GraphVisualization`** was **removed**; `insightsAssessTone` is now a read-only state seeded with **`'delanda'`** and sent on every assess request. `insightsAssessCustomTone` state + validation path were removed with it (custom tones are no longer reachable from the UI; the server still accepts `tone: 'custom'` via direct API use). `INSIGHT_ASSESS_TONE_OPTIONS` is no longer imported by the component. **(2) Reflection↔Discovery balance slider.** New `reflectionBalance` field on **`POST /api/graph-insights-assess`** — numeric **0..100**, default **50**, clamped to `[REFLECTION_BALANCE_MIN, REFLECTION_BALANCE_MAX]`; non-numeric input returns **400 `INVALID_REFLECTION_BALANCE`**. Server helper **`buildReflectionBalanceDirective(n)`** maps the value into one of five LLM directives — **Strongly reflective** (≤20: stay inside the map, inference must cite named concepts), **Leaning reflective** (≤40), **Balanced** (≤60, default), **Leaning discovery** (≤80), **Strongly discovery** (>80: lean into projected / emergent paths, clearly marked as extension). The directive is injected into `userContent` after the guiding-question block and before the output-length block so it composes with **`assessmentLength`** and the existing **two-phase** grounding / extension instructions. Client: new `insightsAssessReflectionBalance` state (default **`INSIGHT_ASSESS_REFLECTION_BALANCE_DEFAULT = 50`**, step **`INSIGHT_ASSESS_REFLECTION_BALANCE_STEP = 5`**) threaded into the `handleInsightsAssess` payload; new `formatInsightAssessReflectionBalance(n)` returns the band label for the readout. New `<input type="range">` + **Reflective** / **Discovery** endpoint spans + band readout sits **between the Question select and the Length select**; `aria-valuemin`/`max`/`now`/`valuetext` set for SR users. **(3) Assess-controls responsive layout.** `.graph-insights-panel__assess-controls` converted from `flex-wrap` to a **CSS Grid** with `grid-template-areas`; mobile (default) stacks **Question → Balance → Length → Assess** vertically, **`≥720px`** media query switches to a single row `"question balance length submit"` (`grid-template-columns: minmax(0, 1.5fr) minmax(0, 1.4fr) minmax(0, 1fr) auto`). Individual widgets gained **`justify-self: stretch`**, trimmed `min-width`s, and the submit button picked up **`min-height: 44px`** to meet the existing **#30** touch-target rule. `.graph-insights-panel__assess-balance*` rules added for label alignment, slider hue, and band readout typography. **Tests:** **`GraphVisualization.test.js`** + **`graphInsights.test.js`** green; **`eslint src`** clean. **Docs:** this entry, **`client/README.md`** (`graphInsights.js` + `#83 Insights` bullets), **`server/READEME.md`** §`lib/graphInsightsAssess.js` (tone list + `reflectionBalance` field + `INVALID_REFLECTION_BALANCE`), **`docs/github-backlog-issues.md`** (*Interactive network insights* follow-ups). **Follow-ups outside this ticket:** (a) **Restore a Perspective picker** under an *Advanced* disclosure — DeLanda is great but users may still want Freud / Palahniuk / Murakami / Thompson on demand (GitHub **#83** / **#96**); (b) **Persist `reflectionBalance` per-graph or per-user** (today it resets to **50** on reload) — candidate for `mindmap.chrome.insightsAssessReflectionBalance` localStorage or `UserActivity` audit on **#96**; (c) **Contract test for `REFLECTION_BALANCE_*` constants** so the client `INSIGHT_ASSESS_REFLECTION_BALANCE_*` values cannot drift from the server (same motivation as **#97** for tone ids); (d) **Snapshot test for `buildReflectionBalanceDirective`** across the five bands (easy unit test, prevents silent wording drift); (e) **Tone parity audit** — with DeLanda added, revisit **#97**'s "shared source of truth for tone ids" proposal so TONE_SYSTEM_HINTS, TONE_IDS, and INSIGHT_ASSESS_TONE_OPTIONS stay in lockstep; (f) **Mobile responsive polish** — grid layout is verified visually; add a Playwright / integration test that clicks through the Insights panel at **≥720px** vs **<720px** breakpoints (**#96** E2E slice); (g) **Custom-tone API preservation** — surface a lightweight way to still use `tone: 'custom'` from the UI (e.g. an *Advanced* block) or add a server-side deprecation decision for custom tones now that the picker is gone.

Last updated: 2026-04-20 — **Explode subgraph — purge-in-place + rewire broken edges:** changed the client behavior of **`handleExplodeNode`** in **`GraphVisualization.js`** so the anchor **is no longer retained after explode** — instead the newly generated Wikipedia-backed cluster **replaces** the anchor in place and the anchor's pre-existing edges are stitched onto the new cluster so the surrounding graph keeps its semantic wiring. Mechanics: (1) snapshot every **external edge** on `data.links` that touched the target in the original graph (anchor endpoint on either side, self-loops excluded); (2) call **`mergeGenerateNodeResponse(data, result.data, width, height, { deletedNodeIds: [targetIdSnapshot] })`** — the merge already filters out any link whose endpoint is in `deletedNodeIds`, which transparently drops both the **old edges on the anchor** and the **server-generated "bridge" edges** (`new → anchor`) from **`ensureExplosionTopology`**, while keeping the **clique among new nodes** intact; (3) **rewire round-robin** — each broken external edge yields a replacement link from the former neighbor to `newNodeIds[idx % newNodeIds.length]`, preserving the **original relationship label** (fallback `'related'`) and **`strength`** (clamped 0..1), with fresh `createdAt`/`timestamp` = `Date.now()` + offset. The server contract for `POST /api/explode-node` is unchanged (anchor-bridge edges still come back — the client just ignores them after the purge). The old `explosionExpandedAt` post-merge marking is removed (the node is gone, so the gate is moot); the two residual call sites that still reference `explosionExpandedAt` (tooltip fragment + explode modal header) are left in place as defensive no-ops — they'll never match after purge. `trackOperation('GENERATE', …)` metadata gains **`deletedNodeIds: [targetNode.id]`** and **`rewiredLinkCount`** alongside the existing `generatedNodes` / `selectedNodes` so telemetry captures the new semantics. **Tests:** `GraphVisualization.test.js` still **16 / 16** green (none of the existing cases asserted anchor persistence); `mergeGenerateResult.test.js` still green (the merge `deletedNodeIds` path was already tested). **`eslint src`** clean (no new warnings). Docs: **`client/README.md`** (Explode bullet rewritten around "purge-in-place" + rewire semantics), this entry in **`docs/status.md`**. **Follow-ups:** (1) add a dedicated unit test for `handleExplodeNode` that asserts the anchor is absent post-merge and the new cluster inherits the original neighbors via the round-robin mapping; (2) consider a **smarter rewire strategy** than round-robin — e.g. attach each broken edge to the new node whose label/description best matches the original relationship, or weight by the old edge's strength; (3) consider surfacing a **"Explode rewires this node's edges"** hint inside the Explode modal so users understand the destructive semantics; (4) decide whether the two residual `explosionExpandedAt` branches should be removed now that they're dead, or retained as a safety net; (5) revisit `docs/github-backlog-issues.md` **#69** note to reflect the new destructive flow.

Last updated: 2026-04-20 — **Extrapolate branch — expose `crossLinksPerIteration` slider + clarify `memoryK`:** the **Extrapolate branch** form in **`GraphVisualization`** (**AI Generation → expansion algorithm `branchExtrapolation`**) now surfaces a second numeric input — **Cross-links per iteration (back-edges into memory)** — bound to new state **`brCrossLinksPerIteration`** (default **0**, min **0**, max **6** to match **`getGenerateBranchCaps().maxCrossLinksPerIteration`**). The value is sent as **`crossLinksPerIteration`** on **`POST /api/generate-branch`** and is also recorded in the operation log via the new **`branchCrossLinksPerIteration`** metadata field alongside the existing **`branchMemoryK`**. When `> 0`, the server's **`buildCrossLinks(newNodes, frontierId, memoryIds, crossCount, ts)`** helper in **`server/lib/generateBranch.js`** adds **deterministic back-edges** from each new node to an earlier memory-window node (non-frontier), labelled **"Cross-link to earlier branch context"**; when `0` (the default), new nodes remain **frontier-only** attached, matching the prior behavior exactly. The adjacent **Memory window** label was relabelled **"Memory window (prompt context, nodes along path)"** with inline helper copy clarifying that `memoryK` **only** feeds context into the LLM prompt (last `k` path nodes + their inter-links) and **does not create edges** — cross-links is the knob for topology. CSS: new lightweight **`.field-help`** rule in **`GraphVisualization.css`** for secondary label copy under numeric inputs (font-size **0.78rem**, color **#555**). **Server contract unchanged** — **`crossLinksPerIteration`** was already validated and threaded through **`executeGenerateBranch`** (**`generateBranchRequest.js`** lines 281–304, 350–361); only the client UI changed. **Tests:** no new test added (the existing server unit test **`validateGenerateBranchRequest ok and dryRun preview`** already covers `crossLinksPerIteration: 1`); full client suite still green; **`eslint src`** clean. **Docs:** **`client/README.md`** (Extrapolate branch bullet rewritten to document both knobs + their distinct roles), **`server/READEME.md`** §5b (`Body` paragraph now lists both `memoryK` as prompt-context-only and `crossLinksPerIteration` as exposed in the UI, up from *"client UI does not expose it"*). **Follow-ups:** consider renaming `memoryK` in the UI to something like **"Prompt memory"** to disambiguate from the back-edge knob; add a sparkline / preview of expected new-edge count per iteration (**`nodesPerIteration + crossLinksPerIteration`**); dry-run preview currently reports only total new nodes — extending it to include cross-link counts would round out the preview story.

Last updated: 2026-04-20 — **Target-node stretch animation generalised across every generation action:** the random-stretch / gentle-reheat pulse that previously ran only during **Explode subgraph** now fires for every node-target generation path — **tooltip Explode**, **tooltip Extend**, and all three **AI Generation** modes from the graph Actions menu (**manual**, **community evolution**, **extrapolate branch**). The function was renamed from **`startExplodeNodeStretchAnimation`** / **`stopExplodeNodeStretchAnimation`** to **`startTargetStretchAnimation`** / **`stopTargetStretchAnimation`** to reflect its new, generation-neutral role; refs renamed **`explodeStretchRef`** → **`targetStretchRef`**, **`explodeStretchTimerStopRef`** → **`targetStretchTimerStopRef`**, **`explodeSimRestoreRef`** → **`targetStretchSimRestoreRef`**. The state shape gained multi-anchor support: **`nodeId: string`** became **`nodeIds: Set<string>`** so multiple highlighted anchors can pulse in sync with a shared stretch scale. A new **`communityDatumContainsAnyGraphNodeId(d, idsSet)`** helper (replacing the single-id variant) drives the sim-tick membership check so merged-community datums still match when any of their member nodes are targets. **Call sites:** **`handleExplodeNode`** → `startTargetStretchAnimation(targetIdStr)`; **`handleExtendNode`** → `startTargetStretchAnimation(targetNode.id)` (new); **`handleGenerate`** → `startTargetStretchAnimation(sourceIdSnapshot)` once Apply validates (new), covering manual (highlighted anchors), community evolution (highlighted anchors if any; silently no-ops on empty), and extrapolate branch (the full highlighted path). Every handler stops the animation in its `finally` block (plus the component unmount cleanup), so it never leaks past a request. Behavior for Explode is unchanged — same ease curve (**`0.09 + sin(…) * 0.04`**), same sim reheat (**`COMMUNITY_SIM_VELOCITY_DECAY_EXPLODE`** / **`COMMUNITY_SIM_CHARGE_EXPLODE`** / **`COMMUNITY_SIM_ALPHA_*_EXPLODE`** constants retained as the shared tuning knobs; renaming them is left as a follow-up since they're now used more broadly than Explode). **Full client suite:** **20 / 121** tests passing; **`eslint src`** clean (0 new errors; the pre-existing `jsx-a11y/anchor-is-valid` warning elsewhere is unchanged). No server changes. **Follow-ups:** consider renaming the shared **`COMMUNITY_SIM_*_EXPLODE`** constants to a neutral prefix (e.g. **`COMMUNITY_SIM_*_TARGET_STRETCH`**) in a future pass; add a dedicated test that asserts the pulse fires for each generation path and stops in `finally`; consider per-node stretch phases (today all pulsing anchors share **`sx/sy`**, so large highlighted sets wobble as one).

Last updated: 2026-04-20 — **Extend + Explode parameters moved into dedicated pop-up modals:** the node-tooltip action buttons **`🌳 Extend 🌳`** and **`💥 Explode 💥`** now each open a **React-rendered modal** (`.modal-overlay` / `.modal-content`) that hosts every parameter for that action — no more inline tooltip forms. **Extend modal** (`graph-extend-modal-*`): mutually-exclusive **Relationship** / **Concept** radio, **200-char** text input, **Guidance** preset + optional Custom textarea (shared React state with Explode and AI Generation); submit fires the existing **`POST /api/generate-node`** flow with **`requiredAnchorId`** = tooltip node, **`numNodes: 1`**, and exactly one of **`requiredRelationshipLabel`** / **`requiredConceptHint`**. **Explode modal** (`graph-explode-modal-*`): **Guidance** preset + optional Custom textarea and the **Concepts to add** slider (**2–6**, default **4**); submit fires **`POST /api/explode-node`**. Server contract unchanged end-to-end. **Tooltip fragments collapsed to just the two buttons** (`extendTooltipFragmentHtml` / `explodeTooltipActionsHtml`); imperative HTML / delegated `change` + `input` listeners and mirror refs (**`extendKindForTooltipRef`**, **`extendTextForTooltipRef`**, **`setExtendKindForTooltipRef`**, **`setExtendTextForTooltipRef`**, **`explodeNumNodesForTooltipRef`**, **`setExplodeNumNodesForTooltipRef`**, **`guidanceForExplodeTooltipRef`**, **`setGuidanceForExplodeTooltipRef`**, **`extendInProgressRef`**) dropped; only the `click` delegated listener remains, and it sets **`extendModal`** / **`explodeModal`** state. **`handleExtendNode`** / **`handleExplodeNode`** now read React state (**`extendTooltipKind`**, **`extendTooltipText`**, **`explodeTooltipNumNodes`**, **`guidancePreset`**, **`guidanceCustomText`**) directly — simpler closures, no more ref-sync dance. CSS: removed `graph-tooltip-(extend|explode)-(kind|text|preset|custom|count)*` inline-form rules; added `.graph-tooltip-modal__*` (anchor header, description, kind radios, primary button) layered on top of the shared `.modal-overlay` / `.modal-content` / `.modal-buttons` pattern so the new modals match the rest of the app. Tests: **`GraphVisualization.test.js`** — the *shows Explode subgraph on the node selection tooltip (#69)* case now asserts the tooltip only has the launch buttons, and that clicking **Explode** opens the modal with the guidance select + count range (**2–6**, default **4**) + submit. Full client suite still **20 / 121** passing; **`eslint src`** clean (no new errors; the pre-existing `jsx-a11y/anchor-is-valid` warning elsewhere is unchanged). Docs: **`client/README.md`** (*Explode subgraph* and *Tooltip → Extend* bullets rewritten around the pop-up UX), **`server/READEME.md`** (`/api/generate-node` UI-surface note + §5c *Client* note both mention the Extend/Explode modals). Follow-ups for a future slice (not in this change): small **visual QA pass** on the modals on narrow viewports (the sliders + textarea still need breathing room around **460 × 634 px**); optional **closing keyboard shortcuts** (Escape already closes via overlay mousedown, but a bound key listener would be nicer); optional **modal enter animation** to match the rest of the app; **relationship-label preset chips** inside the Extend modal (same idea previously scoped for the inline panel — still applicable here).

Last updated: 2026-04-20 — **Single-anchor constraint moved from Manual-generate modal to tooltip `Extend` button:** the earlier in-modal **Anchor constraint** fieldset (Strict vs Single anchor, plus the nested Relationship / Concept sub-constraint) has been **removed from the Manual AI generate modal** and replaced with a dedicated **`➕ Extend ➕`** button in the **docked graph node tooltip**, positioned **above `💥 Explode 💥`**. The tooltip node is used as **`requiredAnchorId`** by construction, so the user only picks **(a)** a constraint kind — **Relationship** (→ **`requiredRelationshipLabel`**) or **Concept** (→ **`requiredConceptHint`**), mutually exclusive and both still capped at **200** chars server-side — **(b)** a free-text value (optional; empty text generates freely while still pinning the anchor). Extend always generates **exactly one** new concept (**`numNodes: 1`** hardcoded) — it is the tight, surgical counterpart to Explode's burst, so no count slider is rendered. Submit calls **`POST /api/generate-node`** with **`expansionAlgorithm: "manual"`**, **`selectedNodes: [<this node>]`**, **`requiredAnchorId`**, **`numNodes: 1`**, and (when typed) exactly one of the two sub-constraints; response merges through **`mergeGenerateNodeResponse`** like other generate flows. **Extend stays enabled even after the node has already been exploded** (independent of **`explosionExpandedAt`**). **Read-only** hides Extend along with Explode. Guidance preset + custom text are **shared with Explode** (same tooltip state → same **`generationContext`**); the **Guidance (optional)** select + Custom textarea now render **inside the Extend panel** (moved up from the Explode section) so they remain reachable even after a node has been exploded — Explode still reads the same shared React state, behavior unchanged. **Server contract unchanged** — **`requiredAnchorId`** / **`requiredRelationshipLabel`** / **`requiredConceptHint`**, **`CONFLICTING_REQUIRED_CONSTRAINTS`**, synthesis-pass force-restore of the verbatim label on new-node → required-anchor edges, and dry-run preview all continue to work exactly as before; the modal just no longer sends any of those fields. **Client (`GraphVisualization.js` / `.css`)**: dropped modal state (**`manualAnchorMode`**, **`manualRequiredAnchorId`**, **`manualConstraintKind`**, **`manualRequiredRelationshipLabel`**, **`manualRequiredConceptHint`**) and the corresponding `graph-generate-required-anchor*` CSS; added tooltip state/refs (**`extendTooltipKind`**, **`extendTooltipText`**, **`extendTooltipNumNodes`**, **`extendInProgress`**) and a new `extendTooltipFragmentHtml` that prepends the Extend panel inside the same `graph-tooltip-explode-wrap` the existing delegated listeners already live on (no new listener wiring needed beyond a handful of class checks). New **`handleExtendNode`** mirrors **`handleExplodeNode`** (error via `window.alert`, **`trackOperation('GENERATE', …)`** with **`requiredAnchorId`** + **`requiredConstraintKind`**). CSS adds `.graph-tooltip-extend-wrap`, `.graph-tooltip-extend-btn`, `.graph-tooltip-extend-kind*`, `.graph-tooltip-extend-text`, `.graph-tooltip-extend-count*`. Full client suite **20 / 121** still green; server **`generateNodeBudget.test.mjs`** unchanged and **31 / 31**. Docs: **`client/README.md`** (new **Tooltip → Extend** bullet + Manual bullet simplified), **`server/READEME.md`** (`/api/generate-node` step 1 gains a **UI surface** note). Follow-ups: visual QA of the Extend panel on small tooltips (range slider vs narrow widths); optional presets dropdown for common relationship labels (same one proposed for the previous modal iteration is still applicable); telemetry dashboard for how often Extend is used vs strict modal vs Explode.

Last updated: 2026-04-20 — **Manual AI generate — single-anchor concept constraint (alternative to relationship label):** extends the same-day single-anchor slice with a mutually-exclusive **concept** constraint. **Server** (`server/lib/generateNodeBudget.js`, `server/server.js`): new **`MAX_REQUIRED_CONCEPT_HINT_CHARS = 200`** export and optional **`requiredConceptHint`** (manual-only, trimmed string). Validation adds **`INVALID_REQUIRED_CONCEPT_HINT`**, **`REQUIRED_CONCEPT_HINT_TOO_LONG`**, and **`CONFLICTING_REQUIRED_CONSTRAINTS`** when both **`requiredRelationshipLabel`** and **`requiredConceptHint`** are sent on the same request. When present, the manual prompt gains a **REQUIRED CONCEPT HINT** bullet inside the REQUIRED ANCHOR block telling the model every new node must be specifically about that subject (while still connecting meaningfully to the required anchor and staying grounded in Wikipedia facts). No synthesis-override is applied to edges for the concept variant — the hint only influences *which* concepts are chosen, not the edge phrasing. Dry-run preview now reports **`requiredConceptHint`**. Outside manual mode (community evolution, branch extrapolation) the field is silently dropped. **Client (`GraphVisualization.js` / `.css`)**: in single-anchor mode, after the **Required anchor** select, a new **Constraint type** radio group lets the user choose **Relationship label — pin the edge phrasing** or **Concept hint — pin the subject of the new nodes**; the matching text input is revealed and the other is hidden, so the payload always carries at most one sub-constraint (no server conflict). New state: **`manualConstraintKind`** (`relationship` | `concept`), **`manualRequiredConceptHint`**. Modal description copy branches on the active constraint kind. Tests: **`server/lib/generateNodeBudget.test.mjs`** adds **6** new cases (accept hint + trim; reject non-string; reject > 200 chars; reject label+hint conflict; randomizedGrowth drops field; dry-run preview carries hint) — **31 / 31** passing in that file. Full client suite: **20 / 121** still green. Docs: **`client/README.md`**, **`server/READEME.md`**, **`docs/github-backlog-issues.md`** (follow-up list updated with presets-dropdown + concept-hint E2E). No change to strict-mode request/response shape.

Last updated: 2026-04-20 — **Manual AI generate — single-anchor + required relationship (follow-up on #37 / #62 / #76):** **`POST /api/generate-node`** (manual mode) accepts two new optional fields — **`requiredAnchorId`** (must equal one of **`selectedNodes[].id`**) and **`requiredRelationshipLabel`** (trimmed string, max **200** chars) — validated in **`server/lib/generateNodeBudget.js`** (new **`MAX_REQUIRED_RELATIONSHIP_LABEL_CHARS = 200`** export and error codes **`REQUIRED_ANCHOR_NOT_SELECTED`**, **`INVALID_REQUIRED_RELATIONSHIP_LABEL`**, **`REQUIRED_RELATIONSHIP_LABEL_TOO_LONG`**). When set, the manual prompt in **`server/server.js`** is rewritten to a **REQUIRED ANCHOR** block: every new node must emit exactly one link with **`target === requiredAnchorId`** using **`requiredRelationshipLabel`** (or a close paraphrase); links to the other highlighted anchors become **optional**, not forbidden. The connectivity check after topology normalization mirrors this rule (only the required edge is required). After the second synthesis pass (**`applySynthesizedRelationships`** → **`synthesizeLinkRelationships`**), the server **force-restores** the verbatim **`requiredRelationshipLabel`** on every new-node → required-anchor edge so the user-pinned phrasing survives the rewrite. Dry-run preview reports **`requiredAnchorId`**, **`requiredRelationshipLabel`**, and a corrected **`estimatedNewLinks`** (equal to **`numNodes`** in single-anchor mode, **`numNodes * selectedCount`** in strict mode). Outside manual (community evolution, branch extrapolation), both fields are silently dropped so stale payloads don't 400. **Client (`GraphVisualization.js` + `GraphVisualization.css`):** new **Anchor constraint** fieldset in the **Manual AI generate** modal with two radio modes — **Strict — each new node connects to every highlighted anchor** (default) and **Single anchor — pin new nodes to one anchor via a chosen relationship**. Single-anchor reveals a **Required anchor** `<select>` populated from highlighted anchors (defaults to the first anchor on mode switch) plus an optional **Required relationship label** `<input type="text" maxLength={200}>` with examples like *"is an example of"*, *"contradicts"*, *"precedes"*. Algorithm description updates to describe the pinned behavior when single-anchor is active. New client-side guard blocks **Apply** if single-anchor is selected without a required anchor. Tests: **`server/lib/generateNodeBudget.test.mjs`** adds **7** new cases (accept label + whitespace trim; reject anchor not in selected set; reject non-string label; reject label > 200 chars; randomized-growth drops both fields; single-anchor dry-run estimates; strict-mode dry-run estimates) — **25** server tests passing in that file. Full client suite: **20 suites / 121 tests passing**. Docs: **`client/README.md`**, **`server/READEME.md`** (Manual strict vs single-anchor + synthesis override), **`docs/github-backlog-issues.md`** (new follow-up under `/api/generate-node`). No change to strict-mode request/response shape; single-anchor is fully opt-in.

Last updated: 2026-04-20 — **Landing page — hero + value-prop cards (branch `feature/76-landing-value-prop-hero`, GitHub #76):** **`LandingPage`** extracted from **`client/src/App.js`** into **`client/src/components/LandingPage.jsx`** (+ **`LandingPage.css`**) and extended with (1) **Hero** — eyebrow *Mindmap*, H1 *See the shape of your thinking.*, tagline drawn from **`ValueProposition.md`**, primary **Get Started** CTA with guest-mode hint; (2) **How it works** three-step loop (preserved from prior landing); (3) **What you can do** — six feature cards with inline SVG icons (ingest text/markdown/audio, analyze + multi-source merge, explore canvas + playback, edit/generate/explode, Insights + centrality, two-phase Assess + voice presets); (4) **Why it matters** reflection blurb with italicized *"how did we get here?"* / *"where was that thought going?"* questions; (5) bottom **Get Started** CTA. Both CTAs call **`useNavigate('/visualize')`** and use the existing **`.landing-cta-primary--dynamic`** pulse (**`prefers-reduced-motion: reduce`** disables animation). Navigation remains in **`GuestIdentityBanner`** — **no Home/Visualize rails duplicated on the page** per **#40** / **#76** scope. Styles: **`LandingPage.css`** adds **`.landing-hero`**, **`.landing-feature-grid`** (responsive **1 / 2 / 3** columns at **≥640 / 960 px**), **`.landing-feature-card`**, **`.landing-reflection`**; existing **`.landing-container`** / **`.landing-steps`** / **`.landing-cta*`** tokens in **`App.css`** untouched. **`ValueProposition.md`** rewritten (*Who it's for*, *What it does*, *Why it matters*, *User story*, *Background*); typos fixed (*Epistemology*, *holistic*, *narratives*, *déjà vu*, *original*, *self-emergent*, *subconscious*, *substantive*); Foucault *episteme* gloss simplified; tangled closing sentence replaced with a concrete tagline. Tests: new **`client/src/components/LandingPage.test.jsx`** (hero + all four headings + six feature cards render; both CTAs navigate to **`/visualize`**); **`App.test.js`** updated to **`getAllByRole`** for multiple *Get Started* CTAs. Full client suite: **20 suites / 121 tests passing**. Docs: **`client/README.md`** (`App.js` + new `LandingPage.jsx` bullet), **`docs/github-backlog-issues.md`** (#76 shipped slice + suggested #76 closure comment + residual follow-ups: screenshots/illustration, A/B copy, analytics, E2E). GitHub: **#76** moved to **in review**.

Last updated: 2026-04-15 — **Graph insights assess — voice presets:** **`jung`** (Carl Jung) replaced by **`palahniuk`** (Chuck Palahniuk); **`freud`** (Sigmund Freud) is the **default** tone in **`GraphVisualization`**. **`TONE_IDS`** / **`INSIGHT_ASSESS_TONE_OPTIONS`**: `freud`, `palahniuk`, `murakami`, `thompson`, `custom`. **Breaking:** **`POST /api/graph-insights-assess`** with **`tone: "jung"`** returns **400** (`INVALID_TONE`). Docs: **`client/README.md`**, **`server/READEME.md`**, **`docs/github-backlog-issues.md`**.

Last updated: 2026-04-15 — **Graph insights assess — prompt (two-phase grounding + extension):** **`POST /api/graph-insights-assess`** / **`server/lib/graphInsightsAssess.js`** — explicit **(A)** ground **factual claims about the map** in the JSON, **(B)** allow **speculative extension** when clearly marked; **psychoanalytic** and **network-lens** framing, guiding questions + output instructions updated; **`assessmentLength`** scales paragraphs and `max_tokens` (**550** / **1100** / **2200**). Client **`INSIGHT_GUIDING_QUESTION_BODY`** / **`INSIGHT_NETWORK_GUIDING_QUESTION_BODY`** in **`graphInsights.js`** stay aligned with server strings. Voice presets: **`INSIGHT_ASSESS_TONE_OPTIONS`** ↔ **`TONE_IDS`** (`freud` default in UI, `palahniuk`, `murakami`, `thompson`, `custom`). Docs: **`client/README.md`**, **`server/READEME.md`**, **`docs/github-backlog-issues.md`** (*Interactive network insights* + follow-ups). GitHub: paste suggested comments on **#83**, **#95**, **#96**, **#97** (see backlog doc); new backlog draft **Insights assess — strict mode & evaluation** if not filed as an issue.

Last updated: 2026-04-15 — **Graph insights assessment (branch `issue-83-graph-insights`):** **`POST /api/graph-insights-assess`** + **`server/lib/graphInsightsAssess.js`** (theme-led prompt); client **`buildGraphInsightAssessPayload`**, voice presets, **Copy** / **Save** / **Close**; **Top by degree** collapsed by default; **`GraphTitleContext`** exported for save filenames. Docs: **`client/README.md`**, **`server/READEME.md`**, **`docs/github-backlog-issues.md`**.

Last updated: 2026-04-15 — **Graph insights #83 v1 (branch `issue-83-graph-insights`):** **View → Insights** in **`GuestIdentityBanner`**; **`GraphChromeUiContext`** **`mindmap.chrome.insightsPanelVisible`** (default off); **`computeGraphInsights`** / **`graphInsights.test.js`**; **`GraphVisualization`** **Network snapshot** panel + **Focus** (same **`discoveryFocusPoint`** / **`applyProgrammaticZoomTransformRef`** / **`applyDiscoveryFocusNodeUiRef`** / rAF×2 as discovery **Focus next**, **#94**). Docs: **`client/README.md`**, **`docs/github-backlog-issues.md`** (*Interactive network insights*). Phase 2 backlog: **`https://github.com/wmmaguire/mind-map/issues/95`**.

Last updated: 2026-04-15 — **Graph discovery #73 slice (branch `issue-73-minimap-pan`):** **Interactive minimap** — click-to-center, drag-to-pan; **`pointer-events: auto`**; **`role="img"`** + **`aria-label`**. **Focus next** — **`discoveryFocusPoint`** (**`graphDiscovery.js`**) aligns pan with **community** layout coords; **`applyProgrammaticZoomTransformRef`** for stable **d3.zoom**; post-focus **selection** + **docked tooltip** via refs only (**`setSelectedNodes` omitted** to avoid D3 effect teardown / **`tooltip.remove()`**). **`updateHighlighting`** treats merged clusters as selected when a member id is in **`selectedNodeIds`**. Tests: **`graphDiscovery.test.js`**. Docs: **`docs/github-backlog-issues.md`** (shipped bullets, follow-up table, **#94** decouple **`selectedNodes`** from D3 deps), **`client/README.md`**. New backlog: **`https://github.com/wmmaguire/mind-map/issues/94`**.

Last updated: 2026-04-15 — **Graph discovery docs (GitHub #73):** Documented **#38** shipped slice vs **#73** phase-2 backlog in **`docs/github-backlog-issues.md`** (*Graph discovery & navigation — #38 shipped / #73 backlog*): `graphDiscovery.js`, **`GraphChromeUiContext`** + **`localStorage`** keys, minimap/search/playback strip, suggested comments for **#38, #33, #39, #51, #52, #57, #70, #40, #73**; draft backlog *Graph minimap — click/drag to pan viewport* if split from #73. **`client/README.md`** module bullets + **#38** sub-bullet under **`GraphVisualization`**. Issue: `https://github.com/wmmaguire/mind-map/issues/73`.

Last updated: 2026-04-15 — **Explode subgraph (GitHub #69, branch `issue-69-explode-node`):** **`POST /api/explode-node`** with **`server/lib/explodeNode.js`** (Wikipedia extract, validated topology, thumbnails, link strength). **Client:** tooltip **Explode** only (removed from Actions AI Generation); **`numNodes`** 2–6; shared **generation guidance**; **`mergeGenerateNodeResponse`**; **`explosionExpandedAt`** gate; stretch/warp + calmer community sim reheat during request; **forceX/forceY** (replacing **forceCenter**) for disjoint-style pull toward viewport center. **Docs:** **`client/README.md`**, **`server/READEME.md`** §5c, **`docs/github-backlog-issues.md`** (*Explode subgraph (#69)* + follow-ups for #24 / #36 / #39 / #51 / #86 / telemetry). **Suggested new backlog:** *Explode subgraph — phase 2* (same doc) if not tracked on #69.

Last updated: 2026-04-15 — **Graph canvas / library playback (#80 branch):** Restored **orange delta highlight** for new nodes and links after a history scrub (timer-based; no D3 transition); **node drag** allowed while scrubbing read-only snapshots (layout only); **link click selection removed** (hover tooltip only); node tooltip **Related concepts** lists show **link strength %** when `link.strength` exists. **Guidance preset** **Funny** → **Simpleton** (`generationGuidance.js`, `GenerationGuidanceFields`; legacy `funny` maps in `resolveGenerationContext`). Docs: **`client/README.md`**, **`docs/github-backlog-issues.md`** (suggested **#36** / **#75** / **#80** comments + follow-ups).

Last updated: 2026-04-14 — **Cluster thumbnail chips (#81):** merged communities render one chip near centroid, anchored to most-connected node; click focuses anchor. **Community evolution (#68):** anchorStrategy + prune + merge. **Library timeline (#86):** smoother scrub transitions. Docs: **`client/README.md`**, **`server/READEME.md`**, **`docs/github-backlog-issues.md`**.

Last updated: 2026-04-13 — **Playback deletions (Apr 2026):** nodes/links now support `deletedAt` so playback can represent removals over time (\([createdAt, deletedAt)\)); delete UI offers **Pop (playback)** vs **Purge (no memory)** via a single in-app modal (avoids `window.confirm` suppression in embedded browsers). `mergePlaybackTimesFromEdit` preserves popped history across subsequent edits at “now”. Graph redraws fade between states (respects `prefers-reduced-motion`). Docs: **`client/README.md`**, **`server/READEME.md`**, and issue follow-ups in **`docs/github-backlog-issues.md`**. **GitHub #75 (branch `issue-75-node-thumbnails`):** Wikipedia **thumbnailUrl** on nodes (REST **page/summary**); **`GET /api/graphs/:filename`** enrichment for load parity with analyze; D3 **`updateVisualization`** on mount/resize + SVG thumb **error** → solid disc; guidance presets (**Awe**, **Nostalgia**, **Shock**, …) bias **concept choice + tone** in **`/api/analyze`** and **`/api/generate-node`** prompts. Docs: **`client/README.md`**, **`server/READEME.md`**, **`docs/github-backlog-issues.md`**. **Landing + guest shell:** **`LandingPage`** on **`/`** — **How it works** (three steps) + **Get Started** → **`/visualize`** (animated CTA, **`prefers-reduced-motion`** respected); **`GuestIdentityBanner`**: **Sign in** chip (**`--auth-sign-in`**), **Create account** line removed from banner (modal only); narrow: icon-only rails + lock icon. Docs: **`client/README.md`**, **`docs/github-backlog-issues.md`**. **Password reset + auth/dev UX (same day):** email **forgot-password** (**`POST /api/auth/forgot-password`**) stores a **1h** one-time token (hashed); **`POST /api/auth/reset-password`** sets a new password and session cookie; **`User`** fields **`passwordResetTokenHash`** / **`passwordResetExpiresAt`**; outbound mail via **nodemailer** (**`server/lib/passwordResetMail.js`**); non-**`smtp(s):`** **`SMTP_URL`** values ignored (web app URL footgun). Client **`GuestIdentityBanner`** “Forgot password?” flow; **`PasswordResetPage`** route **`/reset-password?token=`**; **`AuthContext`** **`requestPasswordReset`** / **`completePasswordReset`**. **Dev:** CRA **`npm start`** forces **`PORT=3000`**; API defaults **`http://localhost:5001`** in development (**`client/src/config.js`**); root **`npm run dev`** sets **`NODE_OPTIONS=--max-http-header-size`**; **`GET /health`** + **`GET /api/test`** registered early in **`server.js`**; HTTP server **`maxHeaderSize`** raised for large cookies (**HTTP 431**). Docs: **`server/READEME.md`** (auth + optional mail env), **`docs/github-backlog-issues.md`** (follow-ups + suggested issue comments). **Production persistence / library UX (Apr 2026):** saved graphs are **Mongo-authoritative** (`Graph.payload`, `metadata.filename`, `metadata.sessionUuid`); **`POST /api/graphs/save`** no longer writes **`server/graphs/*.json`**; **`GET /api/graphs`** / **`GET /api/graphs/:filename`** use Mongo only (no disk fallback). **One-off import:** **`server/scripts/migrate-graphs-to-mongo.js`**. Scoped **`GET /api/files`** omits **`File`** rows whose upload path is missing on disk; **`GET /api/files/:filename`** returns clearer JSON errors (**`FILE_MISSING_ON_DISK`**). Client **`apiRequest`** stringifies object-shaped **`details`**; **`LibraryVisualize`** updates **`currentSource`** after save so banner **SHARE** works; **`GuestIdentityBanner`** z-index above graph Actions FAB. **GitHub #40 (Apr 2026):** **`GraphVisualization`** **empty graph** guidance (`emptyStateVariant` **`library`** / **`default`**, read-only share empty state); shell **Home** / **Visualize** / **Library** / **Upload** rails; **Share link** under **View** menu; **`LibraryAccountChip`** removed (account in banner); library **sidebar** width capped by graph viewport; **mobile** library drawer width + drag-to-close; **graphs** list uses same search/sort as files (**`getFilteredSortedGraphs`**); landing **`LandingPage`** (**How it works** + **Get Started**); shell CTAs remain in **GuestIdentityBanner**. Canvas **tooltip** anchors near the clicked node/link. Docs: **`docs/github-backlog-issues.md`** (#40 follow-ups + suggested issue comments, node thumbnail backlog). Prior **#39** slice (read-only share, hardened Apr 2026): **`POST /api/graphs/:filename/share-read-token`**, **`GET /api/graphs/:filename?shareToken=`** with **`evaluateOwnedGraphRead`** / **`redactGraphMetadataForResponse`** in **`server/lib/graphShareRead.js`**; client **`/visualize?shareGraph=&shareToken=`**, **`LibraryVisualize` `shareViewerMode`**, **`GraphVisualization` `readOnly`**; **`POST /api/graphs/save`** rejects any **`?shareToken=`** (**`SHARE_READ_ONLY`**) and **strips `metadata.shareReadToken`** from save bodies so secrets are **only** minted via share-read-token. HTTP integration test: **`server/routes/graphs.share.integration.test.mjs`** (isolated **`DATA_DIR`**). Docs: **`server/READEME.md`** §6 (share + **future comments/collaboration** permission sketch), **`client/README.md`** (share E2E notes), **`docs/github-backlog-issues.md`** (#39 note + **#74** backlog). **#36** replay: **timestamp-based** playback (**`graphPlayback.js`**, per-entity **`createdAt`** / legacy **`timestamp`**), second shell strip **`GraphPlaybackBanner`** (save + scrubber + speed + share), **`GraphHistoryUiContext`** (`history` / `share` / `save` payloads); identity banner is **title-only**; graph title **blank** when unnamed; **`graphHistory.js`** reducer retained for tests/normalize only; spike **`docs/graph-time-travel-spike.md`**; backlog **#70** + **`docs/github-backlog-issues.md`**. **#62** client polish: Generate modal **Apply**, inline validation, **auto-close** on submit, on-canvas **`graph-edit-mode-chip`** with **progress bar** (indeterminate / per-cycle determinate) + **Stop after this cycle** on the chip; docs in **`client/README.md`**, **`docs/github-backlog-issues.md`**. Still: **#37** **`dryRun`** / budget **Preview** not exposed in the Generate UI (server API unchanged). Earlier same day: **#37** server slice, **#63** auth docs, **#58** follow-ups in **`server/READEME.md`** §2b.
Last updated: 2026-04-13 — **Share link (iOS Safari, Apr 2026):** read-only URL minting unchanged; **`LibraryVisualize`** falls back to an in-app **Share link** modal when **`navigator.clipboard.writeText`** is blocked after the async token mint (common on iOS production). **GitHub #75 (branch `issue-75-node-thumbnails`):** Wikipedia **thumbnailUrl** on nodes (REST **page/summary**); **`GET /api/graphs/:filename`** enrichment for load parity with analyze; D3 **`updateVisualization`** on mount/resize + SVG thumb **error** → solid disc; guidance presets (**Awe**, **Nostalgia**, **Shock**, …) bias **concept choice + tone** in **`/api/analyze`** and **`/api/generate-node`** prompts. Docs: **`client/README.md`**, **`server/READEME.md`**, **`docs/github-backlog-issues.md`**. **Landing + guest shell:** **`LandingPage`** on **`/`** — **How it works** (three steps) + **Get Started** → **`/visualize`** (animated CTA, **`prefers-reduced-motion`** respected); **`GuestIdentityBanner`**: **Sign in** chip (**`--auth-sign-in`**), **Create account** line removed from banner (modal only); narrow: icon-only rails + lock icon. Docs: **`client/README.md`**, **`docs/github-backlog-issues.md`**. **Password reset + auth/dev UX (same day):** email **forgot-password** (**`POST /api/auth/forgot-password`**) stores a **1h** one-time token (hashed); **`POST /api/auth/reset-password`** sets a new password and session cookie; **`User`** fields **`passwordResetTokenHash`** / **`passwordResetExpiresAt`**; outbound mail via **nodemailer** (**`server/lib/passwordResetMail.js`**); non-**`smtp(s):`** **`SMTP_URL`** values ignored (web app URL footgun). Client **`GuestIdentityBanner`** “Forgot password?” flow; **`PasswordResetPage`** route **`/reset-password?token=`**; **`AuthContext`** **`requestPasswordReset`** / **`completePasswordReset`**. **Dev:** CRA **`npm start`** forces **`PORT=3000`**; API defaults **`http://localhost:5001`** in development (**`client/src/config.js`**); root **`npm run dev`** sets **`NODE_OPTIONS=--max-http-header-size`**; **`GET /health`** + **`GET /api/test`** registered early in **`server.js`**; HTTP server **`maxHeaderSize`** raised for large cookies (**HTTP 431**). Docs: **`server/READEME.md`** (auth + optional mail env), **`docs/github-backlog-issues.md`** (follow-ups + suggested issue comments). **Production persistence / library UX (Apr 2026):** saved graphs are **Mongo-authoritative** (`Graph.payload`, `metadata.filename`, `metadata.sessionUuid`); **`POST /api/graphs/save`** no longer writes **`server/graphs/*.json`**; **`GET /api/graphs`** / **`GET /api/graphs/:filename`** use Mongo only (no disk fallback). **One-off import:** **`server/scripts/migrate-graphs-to-mongo.js`**. Scoped **`GET /api/files`** omits **`File`** rows whose upload path is missing on disk; **`GET /api/files/:filename`** returns clearer JSON errors (**`FILE_MISSING_ON_DISK`**). Client **`apiRequest`** stringifies object-shaped **`details`**; **`LibraryVisualize`** updates **`currentSource`** after save so banner **SHARE** works; **`GuestIdentityBanner`** z-index above graph Actions FAB. **GitHub #40 (Apr 2026):** **`GraphVisualization`** **empty graph** guidance (`emptyStateVariant` **`library`** / **`default`**, read-only share empty state); shell **Home** / **Visualize** / **Library** / **Upload** rails; **Share link** under **View** menu; **`LibraryAccountChip`** removed (account in banner); library **sidebar** width capped by graph viewport; **mobile** library drawer width + drag-to-close; **graphs** list uses same search/sort as files (**`getFilteredSortedGraphs`**); landing **`LandingPage`** (**How it works** + **Get Started**); shell CTAs remain in **GuestIdentityBanner**. Canvas **tooltip** anchors near the clicked node/link. Docs: **`docs/github-backlog-issues.md`** (#40 follow-ups + suggested issue comments, node thumbnail backlog). Prior **#39** slice (read-only share, hardened Apr 2026): **`POST /api/graphs/:filename/share-read-token`**, **`GET /api/graphs/:filename?shareToken=`** with **`evaluateOwnedGraphRead`** / **`redactGraphMetadataForResponse`** in **`server/lib/graphShareRead.js`**; client **`/visualize?shareGraph=&shareToken=`**, **`LibraryVisualize` `shareViewerMode`**, **`GraphVisualization` `readOnly`**; **`POST /api/graphs/save`** rejects any **`?shareToken=`** (**`SHARE_READ_ONLY`**) and **strips `metadata.shareReadToken`** from save bodies so secrets are **only** minted via share-read-token. HTTP integration test: **`server/routes/graphs.share.integration.test.mjs`** (isolated **`DATA_DIR`**). Docs: **`server/READEME.md`** §6 (share + **future comments/collaboration** permission sketch), **`client/README.md`** (share E2E notes), **`docs/github-backlog-issues.md`** (#39 note + **#74** backlog). **#36** replay: **timestamp-based** playback (**`graphPlayback.js`**, per-entity **`createdAt`** / legacy **`timestamp`**), second shell strip **`GraphPlaybackBanner`** (save + scrubber + speed + share), **`GraphHistoryUiContext`** (`history` / `share` / `save` payloads); identity banner is **title-only**; graph title **blank** when unnamed; **`graphHistory.js`** reducer retained for tests/normalize only; spike **`docs/graph-time-travel-spike.md`**; backlog **#70** + **`docs/github-backlog-issues.md`**. **#62** client polish: Generate modal **Apply**, inline validation, **auto-close** on submit, on-canvas **`graph-edit-mode-chip`** with **progress bar** (indeterminate / per-cycle determinate) + **Stop after this cycle** on the chip; docs in **`client/README.md`**, **`docs/github-backlog-issues.md`**. Still: **#37** **`dryRun`** / budget **Preview** not exposed in the Generate UI (server API unchanged). Earlier same day: **#37** server slice, **#63** auth docs, **#58** follow-ups in **`server/READEME.md`** §2b.

### Summary
This repo implements a full-stack web app that turns uploaded text/markdown into an interactive “mind map” graph. The architecture is:
- **React (Create React App)** frontend for uploading, selecting files, and visualizing/editing graphs with D3
- **Node.js / Express** backend for file upload, content retrieval, graph generation via OpenAI, and persistence
- **MongoDB (Mongoose)** for sessions, metadata, transforms, graphs, feedback, telemetry, and a **`UserActivity`** audit collection for cross-cutting session/upload/analyze/graph/feedback outcomes (see **`server/READEME.md`**, GitHub **#16**)
- **Filesystem persistence** for uploads + metadata sidecars; **saved graph JSON on disk is legacy** (new saves: Mongo **`Graph.payload`** only). **Which store is authoritative per feature** is summarized under **`server/READEME.md`** → *Data consistency (hybrid persistence)* (GitHub **#20**)
- **Client multi-file library flow:** per-file `POST /api/analyze` plus **`mergeGraphs`** namespaced union in **`client/src/utils/`** (GitHub **#21**); roadmap for connected fusion / splitting in **#47**

The README describes the stack correctly but there is some naming drift: the repo is `mind-map`, while package/app names and production URL references still use `talk-graph`.

---

### Repo structure (high-level)
- `client/`: React app (CRA) with D3 visualization and workflow UI
- `server/`: Express API, Mongoose models, routers, and runtime storage directories:
  - `server/uploads/`: uploaded source files
  - `server/metadata/`: per-upload JSON metadata
  - `server/graphs/`: **legacy** on-disk graph snapshots (optional; import via **`server/scripts/migrate-graphs-to-mongo.js`**)

Root `package.json` provides convenience scripts to run both sides in dev.

---

### Runtime architecture

#### Frontend
- **Router / pages**
  - Routes are defined in `client/src/App.js`:
    - `/`: landing experience + modal upload entrypoint
    - `/visualize`: library + visualization screen

- **Session lifecycle**
  - `client/src/context/SessionContext.jsx` (`SessionProvider`) creates or restores a session via `POST /api/sessions`.
  - The returned session UUID is exposed via `useSession()` and used by:
    - upload requests (`POST /api/upload`)
    - analysis requests (`POST /api/analyze`)
    - telemetry (`POST /api/operations`)
    - graph saving metadata

- **Identity + auth + Library shell (GitHub #31 foundation, #33 UI, #63 auth)**
  - `AuthContext.jsx` / `AuthProvider`: **`/api/auth`** register, login, logout, **`GET /api/me`**, **`PATCH /api/me`**, forgot-password + reset-password helpers (httpOnly **`mindmap_auth`** cookie).
  - `index.js`: **`AuthIdentityBridge`** supplies **`user?.id`** to **`IdentityProvider`** so `useIdentity().userId` matches the signed-in account.
  - `IdentityContext.jsx`: optional `userId` / `isRegistered` / `identityKind`; optional `REACT_APP_MINDMAP_USER_ID`; in **development** with **`REACT_APP_ENABLE_DEV_PREVIEW`**, `setDevRegisteredUserId` for guest preview.
  - `GraphTitleContext.jsx`: `LibraryVisualize` sets the current graph title for the shell banner; cleared when leaving **`/visualize`**.
  - `LibraryUiContext.jsx`: mobile “open library” — `LibraryVisualize` registers visibility + opener; control renders in **`GuestIdentityBanner`** (leading), not a fixed left rail.
  - **`GuestIdentityBanner`**: sign-in / register modal, **forgot password** (email reset link), signed-in menu (user settings, sign out); optional dev preview when env allows; **`onOpenUpload`** from **`App`** opens the root **`FileUpload`** modal; leading **Home** / **Visualize** / **Library** rails and **Upload** chip (**#40**); signed-in account trigger uses the same rail styling (replaces sidebar **`LibraryAccountChip`**). **`PasswordResetPage`** (**`/reset-password`**) for completing reset from email link.
  - **#32** / **#63**: scoped listings; **`POST /api/upload`** sets **`File.userId`** when **`X-Mindmap-User-Id`** is sent. Session-only lists **exclude** account-owned rows so guests cannot see another user’s library after sign-out in the same browser session (**`server/READEME.md`** §3). **Follow-up:** verify header against JWT server-side — **#64** (**`docs/github-backlog-issues.md`**).

- **Core UI flows**
  - **Upload**: `client/src/components/FileUpload.js`
    - **Text:** `multipart/form-data` to `POST /api/upload` (`file`, `customName`, `sessionId`). **Audio → transcript (#34 / #35 / #58):** sub-tabs **Upload file** or **Record** → `POST /api/transcribe` (`audio`, `sessionId`; optional **`verbose`** for segment timestamps). **`verbose`** is exposed in the UI only on the **Record** sub-tab (**Segment timestamps**); **elapsed `m:ss` timer** while recording; switching to **Upload file** clears verbose client state. Then `.txt` via **`POST /api/upload`** (plain text only; timings are UI-only unless persisted in a future ticket).
  - **Library + analyze**: `LibraryVisualize.js` + **`LibrarySidebar`**, **`LibrarySourcesPanel`**
    - Lists files (`GET /api/files`)
    - Reads file content (`GET /api/files/:filename`)
    - Calls analysis (`POST /api/analyze`)
    - Displays graph using `GraphVisualization`
    - Saves/loads graphs (`POST /api/graphs/save`, `GET /api/graphs`, `GET /api/graphs/:filename`)
    - **#36 / #39:** client-side **timestamp** replay (**`graphPlayback.js`**); **`GraphPlaybackBanner`** (save + scrubber strip) + **`GraphHistoryUiContext`**; read-only **share** control on **`GuestIdentityBanner`** (left of **View** on **`/visualize`**). See **`docs/graph-time-travel-spike.md`**
  - **Visualization + editing**: `client/src/components/GraphVisualization.js`
    - D3 force graph rendering
    - Zoom-driven “community” merge/split behavior
    - Add node, add relationship, delete node/link
    - AI expansion via `POST /api/generate-node`
    - Tracks operations via `POST /api/operations`
    - **Graph actions UI** (GitHub **#27**) uses the **Actions** FAB and context menu, not a fixed toolbar. **Dead CSS** for the pre–#27 controls shell was removed in **#28** (`GraphVisualization.css`, `LibraryVisualize.css`). **#29** adds **Generate (AI)** vs **Edit graph** accordion sections (Library-style toggles), **link-flow** copy, a bottom **`graph-edit-mode-chip`** when a modal flow is active **or while AI generation runs** after the Generate modal closes, and scrollable menu height. **#30** adds touch-sized targets and basic a11y for the Actions menu (**`role="group"`**, focus management, **`aria-hidden`** on decorative icons). **#31** (foundation): **`actionsFabPlacement`** — Library uses **`libraryGraphMount`** so the FAB is anchored to the graph panel (absolute over the SVG); default elsewhere is viewport-fixed. See **`docs/github-backlog-issues.md`** and **`client/README.md`**.

#### Backend
- **Entry point**: `server/server.js` (ES modules; server `package.json` uses `"type": "module"`).
- **Environment requirements**
  - `OPENAI_API_KEY` is required; server exits at startup if missing.
  - `MONGODB_URI` is required to connect; connection attempts retry on failure.
- **CORS**
  - Allows `http://localhost:3000` and `https://talk-graph.onrender.com`.

- **Persistence model: hybrid**
  - **Filesystem**
    - Raw uploads saved to `server/uploads/`
    - JSON metadata saved to `server/metadata/`
    - **Saved graphs:** Mongo **`Graph`** documents only (legacy **`graphs/`** files may exist from older deploys)
  - **MongoDB**
    - Used for sessions/user metadata, files, transforms, graphs, feedback, and telemetry.
    - Some flows intentionally “do not fail” if DB writes fail (e.g., upload logs DB error but returns success).

---

### Implemented API surface (major endpoints)

#### Files + uploads
- `POST /api/transcribe` (**#34**, **#58**)
  - Multipart `audio` + `sessionId`; optional **`verbose`** → **`verbose_json`** with **`segments`** / **`duration`**; OpenAI Whisper; `UserActivity` **`TRANSCRIBE_COMPLETE`** (`meta.verbose` when applicable); see **`server/routes/transcribe.js`**
- `POST /api/upload`
  - Upload a file (multer)
  - Validates session exists (by session UUID)
  - Writes metadata JSON file
  - Attempts to store a `File` record in Mongo
- `GET /api/files`
  - With **`?sessionId=`** or **`?userId=`** / **`X-Mindmap-User-Id`**, lists from MongoDB **`File`** (GitHub **#32**) and **drops entries whose file is missing on disk**; otherwise legacy scan of **`server/metadata/*.json`**
- `GET /api/files/:filename`
  - Reads the uploaded file content from `server/uploads/` (**404** + **`FILE_MISSING_ON_DISK`** when absent)

#### Sessions
- `POST /api/sessions`
  - Creates a `Session` with UUID and associated `UserMetadata`
- `POST /api/sessions/:sessionId`
  - Updates session end time and duration (supports sendBeacon style requests)
- `GET /api/sessions/current`
  - Fetches most recent “active” session

#### AI graph generation (OpenAI)
- `POST /api/analyze`
  - Calls OpenAI chat completions; model from **`OPENAI_ANALYZE_MODEL`** (default **`gpt-4o`**)
  - Parses assistant output with shared JSON extraction (handles markdown fences, validates `nodes` / `links`)
  - Creates/updates a `GraphTransform` record (`pending` → `completed`/`failed`)
  - Non-2xx + JSON `details` / `code` on quota/auth or analysis failure
- `POST /api/generate-node`
  - Same model env and JSON parsing approach as analyze
  - **502** + `INVALID_MODEL_JSON` if the model output cannot be parsed to `{ nodes, links }`
  - **429** / **401** for OpenAI quota/auth; connectivity validation may still return `{ success: false }` with **200** (legacy)

#### Graph persistence + analytics
- `POST /api/graphs/save`
  - Saves a graph snapshot to Mongo (**`Graph`**, including **`payload`** and **`metadata.filename`** = `graph_<timestamp>.json`). **#39:** rejects **`?shareToken=`** (**403** / **`SHARE_READ_ONLY`**); strips **`metadata.shareReadToken`** from the body (tokens only via **`POST …/share-read-token`**).
- `GET /api/graphs`
  - Lists **`Graph`** documents; filtered by **`metadata.sessionUuid`** or **`metadata.userId`** when query/header provided (**#32**), else lists all (legacy)
- `GET /api/graphs/:filename`
  - Loads by **`metadata.filename`** from Mongo (**#39:** account-owned graphs need owner header or valid **`?shareToken=`**; response redacts secrets / **`dbId`** for share viewers)
  - Records a `GraphView` when applicable
- `GET /api/graphs/:graphId/views`
  - Returns view statistics for a graph (Mongo)

#### Telemetry and feedback
- `POST /api/operations`
  - Stores a `GraphOperation` record (create/delete/generate/etc.)
- `GET /api/graphs/:graphId/operations`
  - Lists recent operations
- `POST /api/feedback`, `GET /api/feedback`, `GET /api/feedback/view`
  - User feedback capture + retrieval + filtering

---

### Data model overview (MongoDB/Mongoose)
- `Session` (`server/models/session.js`)
  - session UUID, start/end/duration, reference to `UserMetadata`
- `UserMetadata` (`server/models/userMetadata.js`)
  - browser/os/screen/language/timezone
- `File` (`server/models/file.js`)
  - file metadata + path; **`sessionId`**; optional **`userId`** (**#32**)
- `GraphTransform` (`server/models/graphTransform.js`)
  - tracks analysis jobs (status/result), references `Session` and `File`s
- `Graph` (`server/models/graph.js`)
  - persisted graphs: canonical **`payload`** `{ nodes, links }`, **`metadata.filename`**, **`metadata.sessionUuid`**, optional **`metadata.userId`**, share token (**#39**); legacy **`nodes`** / **`links`** subschemas retained for older documents
- `Feedback` (`server/models/feedback.js`)
- `GraphOperation`, `GraphView`
  - analytics/telemetry

---

### Known inconsistencies / risks (current state signals)
- **Naming drift**: many references use “talk-graph” (including production URL), while repo is “mind-map”.
- **Route layout (updated)**:
  - Library uploads and file listing live in `server/routes/files.js`; persisted graphs in `server/routes/graphs.js`. `server/server.js` mounts these and still defines `/api/analyze` and `/api/generate-node` inline.
- **File ↔ session**:
  - Multiple `File` documents per `sessionId` are allowed; `path` is unique per stored artifact. Older DBs may still have a legacy unique index on `sessionId`—drop it if second uploads fail with duplicate key (see `server/READEME.md`).
- **Hard-coded base URLs**:
  - Frontend sometimes uses `window.location.origin`, sometimes hardcodes `https://talk-graph.onrender.com`, and sometimes `http://localhost:5001`, which can make deployment environments brittle.

---

### How to run (from current scripts)
- Root scripts (see `package.json`):
  - `npm run dev`: runs `server` (nodemon) + `client` (CRA) concurrently
  - `npm run start`: runs server only
- Ports:
  - frontend dev server: `http://localhost:3000`
  - backend API: `http://localhost:5001`

Required env vars (backend):
- `OPENAI_API_KEY`
- `MONGODB_URI`

---

### Suggested next documentation additions
- Add a real `server/README.md` describing env vars + Mongo collections + OpenAI expectations.
- Add a diagram showing:
  - upload → metadata/fs + Mongo file record
  - analyze → OpenAI → GraphTransform → visualization → save/load graphs
  - telemetry + feedback flows